# Production environment variables required when building or running the app in production.
# Copy this file to `.env.production` (for local production builds) or use it as a reference
# when configuring variables in your hosting provider (Vercel, Netlify, etc.).

NEXT_PUBLIC_SUPABASE_URL=your-production-project-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_OR_ANON_KEY=your-production-anon-key
