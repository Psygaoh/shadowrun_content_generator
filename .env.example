# Update these with your Supabase details from your project settings > API
# https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# OpenAI credentials for GPT connectivity tests and assistants.
# Required: set to the secret key from https://platform.openai.com/
OPENAI_API_KEY=
# Optional: override the model if needed (defaults to gpt-4o-mini in code).
OPENAI_MODEL=
